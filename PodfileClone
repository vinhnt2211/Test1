# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

use_frameworks!
target 'C2C (AppStore)' do
pod 'GoogleMaps', '1.13.0'
pod 'RSDayFlow', '1.4.1'

pod 'Charts', '2.1.6'

pod 'Fabric', '1.6.7'
pod 'Crashlytics', '3.7.0'
end

target 'C2C (dev)' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for C2C (dev)

end

target 'C2C (TF)' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for C2C (TF)

end
